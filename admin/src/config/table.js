/** Table column min-widths for action cells (multi-language safe). */

export const ACTION_COLUMN = {
  /** Edit + delete */
  basic: 220,
  /** Three link buttons */
  standard: 280,
  /** Order / refund with dropdown */
  order: 260,
  /** System CRUD rows */
  system: 240,
  /** Promotion activity rows */
  promotion: 420,
  /** Product list (many actions) */
  product: 520,
  /** User manage — admin */
  userAdmin: 440,
  /** User manage — non-admin */
  userBasic: 220,
}
